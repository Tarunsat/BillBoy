{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "sql:default",
    {
      "identifier": "sql:allow-load",
      "allow": [{ "db": "sqlite:biller.db" }]
    },
    {
      "identifier": "sql:allow-execute",
      "allow": [{ "db": "sqlite:biller.db" }]
    },
    {
      "identifier": "sql:allow-select",
      "allow": [{ "db": "sqlite:biller.db" }]
    }
  ]
}
